{
  "name": "project-pitwall",
  "private": true,
  "scripts": {
    "dev": "bash ./scripts/dev.sh",
    "dev:infra": "docker compose up -d mysql redis",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:down": "docker compose down"
  }
}
